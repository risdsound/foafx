{
  "name": "foafx-plugin",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "start": "npm run clean && mkdir dist && cp manifest.json dist/elementary.config.json && parcel --no-cache --port 3000 --https --cert localhost-cert.pem --key localhost-key.pem index.html",
    "build": "npm run clean && parcel build --no-source-maps --no-cache --public-url '.' index.html",
    "export": "npm run build && elem-export -m manifest.json -o dist dist/index.html"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "last 2 versions",
      "not dead",
      "not op_mini all",
      "ie 10"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@elemaudio/plugin-renderer": "^1.0.11",
    "@elemaudio/core": "^1.0.3",
    "@parcel/config-default": "^2.5.0",
    "@parcel/transformer-svg-react": "^2.5.0",
    "parcel": "^2.5.0",
    "process": "^0.11.10",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  }
}
